{
  "name": "qamind7",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cy:open:chrome": "npx cypress open --config-file config-dev.js --browser chrome",
    "cy:run:chrome": "npx cypress run --config-file config-dev.js --browser chrome",
    "cy:open:dev": "npx cypress open --config-file config-dev.js --browser chrome",
    "cypress:open:qa": "cypress open --config-file config-qa.js --browser chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^15.2.0",
    "cypress-mochawesome-reporter": "^4.0.2",
    "cypress-multi-reporters": "^2.0.5",
    "dotenv": "^17.2.3",
    "mocha-junit-reporter": "^2.2.1"
  }
}
